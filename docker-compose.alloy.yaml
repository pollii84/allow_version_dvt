services:
  site:
    image: nginx:1.27-alpine
    network_mode: host
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./.alloy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
